/**
 * site.config.js — Single source of truth for the deployed site URL.
 *
 * When you acquire a custom domain (e.g. stormcastph.com), change
 * SITE_URL here and rebuild. Every canonical, sitemap, and OG tag
 * updates automatically.
 */
export const SITE_URL = 'https://stormcast-ph.netlify.app'
